import { Text, View } from 'react-native'
import { db } from '@/config/firebaseConfig'

function TaskList() {
  return (
    <View>
      <Text>Todo list </Text>
    </View>
  )
}

export default TaskList
